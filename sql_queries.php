<?php

// gets the images for the gallery index alon with their info and location

// SELECT photos.id_photo, photos.name, photos.title, photos.text, photos.id_location, locations.location
// FROM photos	
// JOIN locations ON photos.id_location = locations.id_location
// WHERE photos.id_location = 1
// LIMIT 1;

?>